const { JomeBuilder, buildAndRunFile } = require('./builder.js');

// const { compileAndSaveFile } = require('./src/compiler.js');

module.exports = {
  JomeBuilder,
  buildAndRunFile,
  // compileAndSaveFile
}