/* TODO: Enter data in a table. On the left, 3 columns. Code on the left, expected on the middle, actual on the right */

// For example, let's try trimming a string.
function test() {
  
}

// " foo ".trimLeft() | "foo " | "foo "

//~begin table {columns: 3, rows: 10}
  //~A1 `" foo ".trimLeft()`
  //~B1 `" foo"`
  //~C1 `" foo"`
//~end